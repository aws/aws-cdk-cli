# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: test_oidc
on:
  pull_request:
    branches:
      - main
jobs:
  collect:
    runs-on: aws-cdk_ubuntu-latest_4-core
    permissions:
      id-token: write
    steps:
      - run: |
          set -x
          node -p 'JSON.parse(process.argv[1]).value' "$(curl -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" "$ACTIONS_ID_TOKEN_REQUEST_URL&audience=someone")"